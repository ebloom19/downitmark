# Exclude Python virtual environment
venv/
.env.local

# Exclude Git files
.git/
.gitignore

# Exclude node_modules (if necessary)
node_modules/

# Don't exclude pyproject.toml and the api directory
!pyproject.toml
!api/